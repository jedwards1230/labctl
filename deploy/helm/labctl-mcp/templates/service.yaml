apiVersion: v1
kind: Service
metadata:
  name: {{ include "labctl-mcp.fullname" . }}
  labels:
    {{- include "labctl-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "labctl-mcp.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
